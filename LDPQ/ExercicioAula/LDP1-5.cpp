#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <locale>
main()
{
	printf("Alo Mundo!!");
	
	float x, raiz;

    while (x<)
    {
        printf("Digite o valor de x: (sendo positivo)");
        scanf("%.2f", &x);
    }
    raiz=sqrt(x, 2);
    printf("O valor da raiz quadrada de %.2f eh = %.2f", x, raiz);
    


	system("PAUSE");
    
    return 0;
}
